\subsection{Dart}
\subsubsection{命名規約}
\begin{table}[H]
  \centering
  \caption{Dartの命名規約}
  \label{tab:code-dart}
  \begin{tabular}{|c|c|c|} \hline
    対象     & ケース                & 例                \\ \hline \hline
    モジュール名 & snake\_case        & new\_modules \\ \hline
    パッケージ名 & snake\_case        & my\_package      \\ \hline
    クラス名   & UpperCamelCase     & UserCreate       \\ \hline
    関数名    & lowerCamelCase        & getData     \\ \hline
    変数名    & lowerCamelCase        & sampleData       \\ \hline
    定数名    & lowerCamelCase & thisIsStatic \\ \hline
  \end{tabular}
\end{table}

\subsubsection{コーディングスタイル}
Dartのコーディングスタイルは~\href{https://dart.dev/effective-dart/style}{\textbf{Effective Dart}}~に従う。
以下に、主な規約を記載する。

\begin{itemize}
      \item 未使用のコールバック引数には\ 、や\ \_\ を使用する。
      \item プライベートではない識別子はアンダースコア（\_）から始めない。
      \item インデントは2スペース。
      \item 空行の場合はインデントを維持しない。（スペースを削除する）
      \item 引数が2つ以上のWidgetは複数行で書く。
      \item Widgetの引数が1つでも、その子Widgetが複数行の場合は、改行。（1行でWidgetを書くのは、Widgetツリーの末端のみ）
      \item 例外として、Edgelnsetsは複数の引数があっても1行で良い。
      \item 1行が80文字を超えないようにする。
      \item 括弧の終了は、括弧の開始の後に改行しない場合は、同じ行で閉じる。
      \item 括弧の開始の後に改行した場合、
      中身の最終行の次の行で括弧を閉じる。
      尚、インデントはブロック開始時と同じにする。
      \item 関数の呼び出しで「名前なし引数」で開始する場合、
      同じ行でWidgetの括弧を開始する。
      Widgetの閉じる括弧と関数呼び出しの閉じる括弧は同じ行。
      \item 関数の呼び出しで「名前あり引数」の場合は、
      Widgetと同様に記述。
      \item \verb|//|\ を使用。
      \item コメント開始\ \verb|//|\ とコメント本文のは半角スペースひとつ開ける。
      \item オブジェクト生成時の new は省略する。 const は省略しない。
      \item 初期化値の代入以後で変更（再代入）しない変数は、
      final で宣言する。
      \item 2つの値のどちらかを得るだけの場合など、
      単純な場合は使用して良い。
      結果にさらに式を含まないようにする。
      \item 三項演算子のなかで使うものを予め計算するか、
      別にメソッドなどに切り分ける。
      \item dart\:から始まるインポートは他のインポートよりも前に置く。
      \item package\:から始まるインポートは相対的なインポートよりも前に置く。
      \item エクスポートはインポートとは別のセクションで指定する。
      \item インポートやエクスポートは辞書順に並べ替える。
      \item 1行が81文字以上になることを避ける。
      \item ifやforの\{\}を省略しない。
      \item 例外として、ifにelseがなく、
      かつ、全てが1行に収まる場合は{}は省略しても良い。

\end{itemize}


\subsection*{・ツール}
コード作成時に自動でこれらのDart規約に従うように、
以下のツールを導入し調整を行う。\par
\begin{itemize}
  \item[-] VScodeの設定である”format on save”を有効化にし、自動的にフォーマットを行う
  \item[-] dart fix \verb|--|applyでコーディング規約に沿うように変換を行うコマンド
  \item[-] flutter analyzeでコーディング規約違反の有無を表示するコマンド
\end{itemize}

\clearpage
